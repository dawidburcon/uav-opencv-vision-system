[project]
name = "uav-opencv-vision-system"
version = "0.1.0"
description = "UAV-rover vision system: ArUco marker detection, distance estimation, Valkey-based coordination"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["common", "uav_system", "server"]

[tool.uv]
package = false