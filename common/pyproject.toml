[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "redis>=8.0.1",
    "numpy>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
